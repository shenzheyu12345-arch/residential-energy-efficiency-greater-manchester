# Verification outputs

These files are retained from the final dissertation run so that a reproducing researcher can compare sample counts, descriptive summaries, spatial statistics, model diagnostics, SHAP summaries and sensitivity results.

The directory deliberately excludes raw property-level data, document-rendering QA, draft outputs and exploratory analyses that were not reported. The `figures` subdirectory contains the nine formal figures used in the dissertation and its analytical appendix.

